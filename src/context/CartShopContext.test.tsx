//import { renderHook, act } from '@testing-library/react-hooks';
import { CartShopProvider, useCart } from './CartShopContext';

test('should calculate total correctly', () => {
  // const { result } = renderHook(() => useCart(), { wrapper: CartShopProvider });

  // act(() => {
  //   result.current.addToCart({ id: 1, name: 'Product 1', price: 100, quantity: 2 });
  //   result.current.addToCart({ id: 2, name: 'Product 2', price: 150, quantity: 1 });
  // });

  // expect(result.current.total).toBe(350);
});
